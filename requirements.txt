pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
# Optional modern boosting baseline. Pipeline skips LightGBM Return if absent.
lightgbm>=4.0.0
prophet>=1.1.4
tensorflow>=2.15.0
ta>=0.11.0
matplotlib>=3.7.0
joblib>=1.3.0
optuna>=3.0.0
protobuf<7
yfinance>=0.2.36
requests>=2.31.0
pandas-datareader>=0.10.0   # FRED API -> CPI verisi; faiz TCMB EVDS'den alinir

# ── PyTorch (TFT için) ─────────────────────────────────────────────────────
# CPU için:
#   pip install torch
# CUDA 12.8 (RTX serileri) için:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
statsmodels>=0.14.0
shap>=0.45.0
